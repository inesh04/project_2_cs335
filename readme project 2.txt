readme project 2 
Ines Hoxha 
csci 335 spring 2026
unordered_set is the fastest for contains() with a time of O(1).
AVL tree is the slowest for contains(), since we have to do a name search for every node.
we use the has set when we need the fastest time.
we are using the tree when the range queries are the most important